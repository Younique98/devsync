# Template only - do NOT commit a filled-in version of this file.
# Copy to secrets.yaml, fill in real values base64-encoded
# (echo -n 'value' | base64), and apply with:
#   kubectl apply -f k8s/secrets.yaml
apiVersion: v1
kind: Secret
metadata:
  name: devsync-secrets
  namespace: devsync
type: Opaque
data:
  PG_USER: ''
  PG_PASSWORD: ''
  PG_DATABASE: ''
  MONGO_URI: ''
  VAULT_TOKEN: ''
